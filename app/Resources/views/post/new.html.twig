{% extends 'back.html.twig' %}

{% block content %}
    <h1>Post creation</h1>

    {{ form_start(form) }}
        <div class="row">
            <div class="col-md-8">
                <div class="from-group">
                    {{ form_row(form.title,{'attr':{'class':'form-control'}}) }}
                </div>
                  <div class="from-group">
                    {{ form_row(form.description,{'attr':{'class':'form-control'}}) }}
                </div>
                <div class="from-group">
                    {{ form_row(form.slug,{'attr':{'class':'form-control'}}) }}
                </div>
              
                <div class="from-group">
                    {{ form_row(form.body,{'attr':{'class':'form-control'}}) }}
                </div>
                
                
            </div>
            <div class="col-md-4">
                <div class="from-group">
                    {{ form_row(form.image,{'attr':{'class':'form-control'}}) }}
                </div>
                  <div class="form-group">
                    {{ form_row(form.datepublish,{'attr':{'class':'form-control'}}) }}
                </div> 
                  <div class="form-group">
                    {{ form_row(form.categories,{'attr':{'class':'form-control'}}) }}
                  </div>


            <input class="btn btn-primary" type="submit" value="Enregistrer" />        
            <a class="btn btn-warning" href="{{ path('post_index') }}">Retour</a>
            </div>
        </div>
 
    {{ form_end(form) }}

   
{% endblock %}
